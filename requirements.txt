# Pinned to the EXACT versions the model was trained and the app validated on.
# scikit-learn, numpy and joblib MUST match the training environment, or
# unpickling artifacts/pipeline.pkl can fail or behave incorrectly.
streamlit==1.58.0
plotly==6.8.0
scikit-learn==1.8.0
numpy==2.4.4
joblib==1.5.3
pandas==3.0.2
matplotlib==3.10.8
